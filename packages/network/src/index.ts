import {
  initNetwork,
  request,
} from '@/instance';

import {
  createApiMethod
} from '@/createApiMethod';

import {
  RequestConfig,
  RequestResponse,
} from '@/types';


export {
  // instance
  initNetwork,
  request,
  // createApiMethod
  createApiMethod,
};

export type {
  RequestConfig,
  RequestResponse
};