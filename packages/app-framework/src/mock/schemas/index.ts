import { Schema } from 'render-engine';

const schema = {
  ComponentType: 'Group',
  Children: [
    {
      ComponentType: 'Title',
      Props: {
        text: 'IndexPage'
      }
    }
  ],
} as Schema;

export default schema;